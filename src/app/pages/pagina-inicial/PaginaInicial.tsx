import { Link } from "react-router-dom"
import { Login } from "../login/Login"

export const PagingInitial = () =>{
    return(
        <div>
            <h1>initio</h1>
            <Link to="/login"> login</Link>

            
        </div>
    )
}